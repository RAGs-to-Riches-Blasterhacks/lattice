[build]
builder = "nixpacks"
buildCommand = "cd Backend/FastAPI && pip3 install -r requirements.txt"

[deploy]
startCommand = "cd Backend/FastAPI && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
